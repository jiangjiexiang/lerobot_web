/**:
  ros__parameters:
    # The executable exposes these as CLI arguments today. These names document
    # the stable integration boundary for vendor-specific robot drivers.
    leader_state_topic: /leader/joint_states
    follower_state_topic: /follower/joint_states
    command_topic: /follower/joint_trajectory_controller/joint_trajectory
